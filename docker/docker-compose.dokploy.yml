services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: sentient-backend
    ports:
      - "5200:5000"
    environment:
      PYTHONUNBUFFERED: "1"
      SENTIENT_CONFIG_PATH: "/app/sentient.yaml"
      # FLASK_ENV, LOG_LEVEL и т.п. НЕ указываем здесь — задавай в Dokploy ENV UI
    volumes:
      - ../sentient.yaml:/app/sentient.yaml:ro
      - ../src:/app/src:ro
      - ../project_results:/app/project_results
      - ../emergency_backups:/app/emergency_backups
      - ../logs:/app/logs
      - ../local_data:/app/local_data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
    container_name: sentient-frontend
    ports:
      - "3000:3000"
    environment:
      DOCKER_ENV: "true"
      # VITE_API_URL задай в Dokploy ENV UI (см. заметку ниже)
    volumes:
      - ../frontend/src:/app/src:ro
      - ../frontend/public:/app/public:ro
      - ../frontend/index.html:/app/index.html:ro
      - ../frontend/vite.config.ts:/app/vite.config.ts:ro
      - ../frontend/tsconfig.json:/app/tsconfig.json:ro
      - ../frontend/tailwind.config.js:/app/tailwind.config.js:ro
      - ../frontend/postcss.config.js:/app/postcss.config.js:ro
    depends_on:
      - backend
